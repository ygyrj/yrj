<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysLogininforMapper">
<!--YRJ（4.17）-->
	<resultMap id="SysLogininforResult" type="SysLogininfor">
		<id property="infoId" 			column="info_id"/>
		<result property="loginName" 	column="login_name"/>
		<result property="status"		column="status"/>
		<result property="ipaddr"		column="ipaddr"/>
		<result property="loginLocation" column="login_location"/>
		<result property="browser" 		column="browser"/>
		<result property="os"			column="os"/>
		<result property="msg"			column="msg"/>
		<result property="loginTime"	column="login_time"/>
	</resultMap>

	<insert id="insertLogininfor" parameterType="SysLogininfor">
		insert into sys_logininfor (login_name, ipaddr, login_location, browser, os, status,
									msg, login_time)
		values (#{loginName}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{status}, #{msg}, #{loginTime})
	</insert>

	<select id="selectLogininforList" parameterType="SysLogininfor" resultMap="SysLogininforResult">
		select info_id, login_name,ipaddr,login_location,browser,os,status,msg,login_time
		from sys_logininfor
		<where>
			<if test="ipaddr !=null and ipaddr !=''">
				and ipaddr like concat('%',#{ipaddr},'%')
			</if>
			<if test="loginName !=null and loginName != ''">
				and login_name like concat('%',#{loginName},'%')
			</if>
			<if test="status !=null and status !='' ">
				status = #{status}
			</if>
			<if test="params.beginTime !=null and params.beginTime !=''"><!--开始检索时间 &gt mybatis中小于 -->
				and date_format(login_time,'%y%m%d') &gt;=
				date_format(#{params.beginTime},'%y%m%d')
			</if>
			<if test="params.endTime != null and params.endTime !=''"><!--结束检索时间 &lt 小于-->
				and date_format(login_time,'%y%m%d') &lt;=
				date_format(#{params.endTime},'%y%m%d')
			</if>
		</where>
	</select>

	<delete id="deleteLogininforByIds" parameterType="String">
		delete from sys_logininfor where info_id in
		<foreach collection="array" item="infoId" open="(" separator="," close=")">
			#{infoId}
		</foreach>
	</delete>

	<update id="cleanLogininfor">
		<!--truncate 截断清空表，delete时会触发与表相关的触发器，而truncate不会，delete可以加where条件-->
		truncate table sys_logininfor
	</update>
    
</mapper> 